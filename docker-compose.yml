version: '3.8'

services:
  backend:
    build: .
    container_name: innovatech-backend
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "8081:8081"
    volumes:
      - backend_data:/app/data

volumes:
  backend_data: